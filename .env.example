# Copia este archivo a ".env" y pon la URL real de tu backend.
# OJO: incluye el prefijo /api/v1 porque tus rutas lo usan.
# Ejemplo local:
VITE_API_BASE_URL=http://localhost:8080/api/v1
# Ejemplo en Railway (reemplaza por tu dominio real):
# VITE_API_BASE_URL=https://skillmatch-production.up.railway.app/api/v1
